extends layout

block content
  h1= title 
  ul
    each user in users 
      li
        h4 #{user.name}    
        span #{user.paw}
        span #{user.meta.createAt}
        span #{user.meta.updateAt}